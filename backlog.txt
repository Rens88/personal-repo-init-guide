- Under 8, the suggested text is a agent prompt, not a shell command. In that case it does not have to have the switch powersehll/cmd/bash/wsl. Check the whole html: Only show powershell / cmd / bash/wsl switch if it is indeed a shell command, and not when it is a suggsted prompt.

expand explanation for using sbx --clone (for example: How can the user access the files in this cloned repo, how should the user commit, push and create a PR for this cloned repo (explain both the manual and the agent options)