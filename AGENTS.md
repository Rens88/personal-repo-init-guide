This specific library is where I want to keep track of my personal standalone html checklist that helps me set-up new coding projects.

The .files in src/ can be ignored for the purpose of maintaining/updating the current repository. Those files serve the purpose of initating OTHER repositories.

The produced .html (in dist/) must be entirely standalone. In future, I want to share it with students from workshops so they can follow my "stack" if the want to.